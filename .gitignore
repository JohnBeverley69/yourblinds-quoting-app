# --- Secrets ---
.env
.env.local
.env.*.local

# --- Claude Code local state ---
.claude/

# --- Local-only data ---
# Production / legacy DB dumps live locally, never in source control
database/live_dump*.sql
database/*_dump*.sql
database/*.dump

# Auto-snapshots taken by /master-admin/backup.php before each restore.
# Contains real tenant data — must never be committed.
/_backups/*
!/_backups/.htaccess
!/_backups/.gitkeep

# --- Dependencies ---
/vendor/
/node_modules/

# --- Build / cache ---
/cache/
/tmp/
*.log

# --- User uploads (runtime data, not source) ---
# Folder is gitignored, but the .htaccess + .gitkeep that establish the
# folder + its PHP-execution lockdown ARE tracked.
/uploads/*
!/uploads/.htaccess
!/uploads/.gitkeep
/public/uploads/

# --- IDE / editor ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- OS ---
.DS_Store
Thumbs.db
desktop.ini

# --- Composer ---
composer.phar

# --- Personal / non-app files kept in the working dir (not part of the app) ---
/Beverley-Blinds-Flow.pdf
/CNC-Copy-Tool/
/Man-Cave-Cinema-Speaker-Layout.pdf
/Renault-Trafic-Dealer-Handout.pdf
/Renault-Trafic-Dealer-Handout.py
/Timber-Workshops-Foundation-Plan*.pdf
/Workshops-Pad-*.pdf
